---
const year = new Date().getFullYear();
---

<aside class="footer-ebook">
  <div class="container footer-ebook-inner">
    <img
      src="/images/taie-cover.webp"
      width="1414"
      height="2000"
      alt="Teaching AI Ethics eBook cover"
      class="footer-ebook-cover"
      loading="lazy"
    />
    <div class="footer-ebook-text">
      <p class="footer-ebook-label">Free eBook</p>
      <p class="footer-ebook-title">Teaching AI Ethics: A Guide for Educators</p>
    </div>
    <a href="https://subscribe.leonfurze.com/teaching-ai-ethics" target="_blank" rel="noopener noreferrer" class="footer-ebook-btn">Download</a>
  </div>
</aside>

<footer class="site-footer">
  <div class="container footer-inner">
    <div class="footer-top">
      <div class="footer-brand">
        <span class="footer-name">Teaching AI Ethics</span>
        <p class="footer-tagline">AI Ethics in Education</p>
      </div>
      <nav class="footer-links" aria-label="Footer navigation">
        <a href="/">Home</a>
        <a href="/articles">Articles</a>
        <a href="/resources">Resources</a>
        <a href="/about">About</a>
        <a href="/contact">Contact</a>
      </nav>
    </div>
    <hr class="divider" />
    <div class="footer-bottom">
      <p class="footer-copy">&copy; {year} Teaching AI Ethics. All articles released under <a href="https://creativecommons.org/licenses/by-nc-sa/4.0/deed.en" target="_blank" rel="noopener">CC BY-NC-SA 4.0</a>.</p>
      <p class="footer-credit">
        Based on the original collection from <a href="https://leonfurze.com" target="_blank" rel="noopener">Leon Furze</a>. Images from <a href="https://betterimagesofai.org" target="_blank" rel="noopener">Better Images of AI</a> (CC-BY 4.0).
      </p>
    </div>
  </div>
</footer>

<style>
  /* Footer eBook CTA */
  .footer-ebook {
    margin-top: var(--space-xl);
    padding: var(--space-md) 0;
    border-top: 1px solid var(--border-subtle);
  }

  .footer-ebook-inner {
    display: flex;
    align-items: center;
    gap: var(--space-md);
  }

  .footer-ebook-cover {
    width: 56px;
    height: auto;
    border-radius: 3px;
    flex-shrink: 0;
    box-shadow: 2px 3px 8px rgba(0, 0, 0, 0.3);
  }

  .footer-ebook-text {
    flex: 1;
    min-width: 0;
  }

  .footer-ebook-label {
    font-size: 0.6875rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--accent-warm);
    font-weight: 600;
    margin-bottom: 0.125rem;
  }

  .footer-ebook-title {
    font-family: var(--font-display);
    font-size: 1rem;
    font-style: italic;
    font-weight: 400;
    line-height: 1.3;
    margin-bottom: 0;
  }

  .footer-ebook-btn {
    flex-shrink: 0;
    display: inline-flex;
    align-items: center;
    padding: 0.5rem 1.25rem;
    border-radius: 100px;
    font-size: 0.8125rem;
    font-weight: 600;
    background: var(--accent);
    color: var(--btn-text);
    transition: all var(--transition-fast);
  }

  .footer-ebook-btn:hover {
    background: var(--accent-hover);
    color: var(--btn-text);
    transform: translateY(-1px);
    box-shadow: 0 4px 12px rgba(45, 212, 168, 0.25);
  }

  @media (max-width: 640px) {
    .footer-ebook-inner {
      flex-wrap: wrap;
      gap: var(--space-sm);
    }

    .footer-ebook-text {
      flex: 1 1 calc(100% - 72px);
    }

    .footer-ebook-btn {
      width: 100%;
      justify-content: center;
    }
  }

  .site-footer {
    padding: var(--space-lg) 0 var(--space-md);
    border-top: 1px solid rgba(45, 212, 168, 0.1);
    position: relative;
  }

  .site-footer::before {
    content: '';
    position: absolute;
    top: -1px;
    left: 50%;
    transform: translateX(-50%);
    width: 120px;
    height: 1px;
    background: var(--accent);
    box-shadow: 0 0 12px var(--accent-glow);
  }

  .footer-inner {
    display: flex;
    flex-direction: column;
    gap: 0;
  }

  .footer-top {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
  }

  .footer-name {
    font-family: var(--font-display);
    font-weight: 400;
    font-size: 1.125rem;
    font-style: italic;
  }

  .footer-tagline {
    color: var(--text-muted);
    font-size: 0.8125rem;
    margin-top: 0.25rem;
    margin-bottom: 0;
  }

  .footer-links {
    display: flex;
    gap: var(--space-sm);
  }

  .footer-links a {
    color: var(--text-muted);
    font-size: 0.8125rem;
    font-weight: 500;
    transition: color var(--transition-fast);
  }

  .footer-links a:hover {
    color: var(--accent);
  }

  .footer-bottom {
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-wrap: wrap;
    gap: var(--space-xs);
  }

  .footer-copy {
    color: var(--text-muted);
    font-size: 0.75rem;
    margin-bottom: 0;
  }

  .footer-copy a {
    color: var(--text-muted);
    text-decoration: underline;
    text-underline-offset: 2px;
    transition: color var(--transition-fast);
  }

  .footer-copy a:hover {
    color: var(--accent);
  }

  .footer-credit {
    margin-bottom: 0;
    font-size: 0.75rem;
    color: var(--text-muted);
  }

  .footer-credit a {
    color: var(--text-muted);
    text-decoration: underline;
    text-underline-offset: 2px;
    transition: color var(--transition-fast);
  }

  .footer-credit a:hover {
    color: var(--accent);
  }

  @media (max-width: 640px) {
    .footer-top {
      flex-direction: column;
      gap: var(--space-sm);
    }

    .footer-bottom {
      flex-direction: column;
      align-items: flex-start;
    }
  }
</style>
